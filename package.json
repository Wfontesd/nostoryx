{
  "name": "nostoryx",
  "version": "0.1.0-lab",
  "private": true,
  "type": "module",
  "description": "NOSTORYX developer labs for isolated MMORPG systems testing.",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test",
    "ci": "npm run check && npm test && npm run build"
  },
  "engines": {
    "node": ">=22"
  }
}
